INSERT INTO SOLDITEM (SALE_ID, STOCKITEM_ID, NAME, QUANTITY, ITEMPRICE)
SELECT (SELECT ISNULL(max(SALE_ID) + 1, 1) FROM SOLDITEM) AS SALE_ID, ID, NAME, FLOOR( 1 + RAND( ) * 6), PRICE FROM STOCKITEM limit 5;
INSERT INTO SOLDITEM (SALE_ID, STOCKITEM_ID, NAME, QUANTITY, ITEMPRICE)
SELECT (SELECT ISNULL(max(SALE_ID) + 1, 1) FROM SOLDITEM) AS SALE_ID, ID, NAME, FLOOR( 1 + RAND( ) * 6), PRICE FROM STOCKITEM limit 5;
INSERT INTO SOLDITEM (SALE_ID, STOCKITEM_ID, NAME, QUANTITY, ITEMPRICE)
SELECT (SELECT ISNULL(max(SALE_ID) + 1, 1) FROM SOLDITEM) AS SALE_ID, ID, NAME, FLOOR( 1 + RAND( ) * 6), PRICE FROM STOCKITEM limit 5;
INSERT INTO SOLDITEM (SALE_ID, STOCKITEM_ID, NAME, QUANTITY, ITEMPRICE)
SELECT (SELECT ISNULL(max(SALE_ID) + 1, 1) FROM SOLDITEM) AS SALE_ID, ID, NAME, FLOOR( 1 + RAND( ) * 6), PRICE FROM STOCKITEM limit 5;
INSERT INTO SOLDITEM (SALE_ID, STOCKITEM_ID, NAME, QUANTITY, ITEMPRICE)
SELECT (SELECT ISNULL(max(SALE_ID) + 1, 1) FROM SOLDITEM) AS SALE_ID, ID, NAME, FLOOR( 1 + RAND( ) * 6), PRICE FROM STOCKITEM limit 5;